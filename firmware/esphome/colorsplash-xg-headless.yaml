---
# ColorSplash XG → ESPHome bridge, headless variant.
#
# Targets a plain ESP32-WROOM-32 dev board placed near the
# equipment pad, where BLE link margin to the LPL-XG-CTRL-1 is
# strong. There's no display — the user-facing UI lives in Home
# Assistant (see ../../dashboard/colorsplash-xg.yaml for the
# stock-Lovelace card).
#
# Includes everything from the display variant except the LVGL
# UI, the RGB/touch panel, and the S3-specific PSRAM/IDF
# tunings. Shares the custom `colorsplash_xg` BLE component.
#
# For the touchscreen variant (Waveshare ESP32-S3-Touch-LCD-7),
# see colorsplash-xg-display.yaml or check out the v0.1.0-display
# tag.
#
# See firmware/esphome/README.md for setup and flashing steps.

substitutions:
  device_name: colorsplash-xg-bridge
  friendly_name: ColorSplash XG Bridge

esphome:
  name: ${device_name}
  friendly_name: ${friendly_name}
  min_version: 2026.4.0

esp32:
  # Plain ESP32-WROOM-32 dev kit. ESPHome's default `arduino`
  # framework is fine here — `ble_client` + Wi-Fi + native API +
  # the custom `colorsplash_xg` component all build cleanly
  # without any IDF-specific options.
  board: esp32dev
  # Standard WROOM-32 modules ship 4 MB. Some carry 8 or 16 MB —
  # adjust if `esptool flash_id` reports differently.
  flash_size: 4MB

logger:
  level: INFO
  baud_rate: 115200

api:
  encryption:
    key: !secret api_key
  # Phase 4b show-scrub proof-of-concept: a single primitive
  # exposed as a Home Assistant service. Sends `start_byte`,
  # waits `wait_ms`, then sends Lock (0x0d). With a determinist-
  # ic transition + fixed step duration per show, repeated calls
  # to (start_byte, wait_ms) should land the fixture on the same
  # color each time. Used to validate that show-scrub is
  # precise enough for an arbitrary-RGB picker (Phase 4b proper)
  # before building the full RGB→(show, offset) mapping.
  #
  # For repeatability, normalise the from-state by sending
  # Standby first and pausing ~3 s before scrub:
  #
  #   - service: button.press
  #     target: { entity_id: button.pool_standby }   # if exposed
  #   - delay: 3
  #   - service: esphome.colorsplash_xg_bridge_pool_scrub
  #     data: { start_byte: 5, wait_ms: 12000 }      # 0x05 = Patriot Dream
  services:
    - service: pool_scrub
      variables:
        start_byte: int
        wait_ms: int
      then:
        - script.execute:
            id: pool_scrub_seq
            start_byte: !lambda 'return start_byte;'
            wait_ms: !lambda 'return wait_ms;'
    # Phase 4b calibration primitive — send a single arbitrary byte
    # to the controller. Used by `tools/calibrate_show_colors.py`
    # to drive the controller through Standby + every solid +
    # every show without needing per-byte HA buttons.
    - service: pool_send_byte
      variables:
        byte: int
      then:
        - lambda: 'id(xg).send_effect_byte((uint8_t) byte);'
    # Phase 4b production picker: take an arbitrary observed RGB
    # target and drive the fixture to display it. The component's
    # find_recipe + pick_color do the LUT lookup against the
    # embedded show_color_lut.h (generated from
    # tools/show_colors_video.json) and dispatch either a
    # single-byte solid send or a start-byte → Lock scrub sequence.
    # The lock-comp 700ms is baked into the component default.
    - service: pool_set_rgb
      variables:
        red: int
        green: int
        blue: int
      then:
        - lambda: |-
            id(xg).pick_color((uint8_t) red, (uint8_t) green,
                              (uint8_t) blue);

    # User color presets (issue #53). Each preset is keyed by a
    # `slug` ([a-z0-9_], up to 15 chars) so HA automations can
    # call:
    #   service: esphome.colorsplash_xg_bridge_color_preset_recall
    #   data: {slug: sunset_magenta}
    # The hex (r,g,b) is stored alongside the (start_byte,
    # wait_ms) recipe so the JS card can render preview swatches
    # and HA dashboards can show them without re-running the
    # picker.
    - service: color_preset_save
      variables:
        slug: string
        name: string
        red: int
        green: int
        blue: int
        start_byte: int
        wait_ms: int
      then:
        - lambda: |-
            id(xg).color_preset_save(
                slug, name,
                (uint8_t) red, (uint8_t) green, (uint8_t) blue,
                (uint8_t) start_byte, (uint32_t) wait_ms);
            // Republish all 5 indexed slot sensors + count.
            // Count published LAST so the card never sees count=N
            // pointing at a slot whose state hasn't propagated.
            id(pool_color_preset_0).publish_state(id(xg).preset_at_json(0));
            id(pool_color_preset_1).publish_state(id(xg).preset_at_json(1));
            id(pool_color_preset_2).publish_state(id(xg).preset_at_json(2));
            id(pool_color_preset_3).publish_state(id(xg).preset_at_json(3));
            id(pool_color_preset_4).publish_state(id(xg).preset_at_json(4));
            id(pool_color_preset_count).publish_state(
                (float) id(xg).preset_count());

    - service: color_preset_recall
      variables:
        slug: string
      then:
        - lambda: 'id(xg).color_preset_recall(slug);'

    - service: color_preset_delete
      variables:
        slug: string
      then:
        - lambda: |-
            id(xg).color_preset_delete(slug);
            // Republish all 5 slot sensors + count (delete shifts
            // entries down, so multiple slot states change).
            id(pool_color_preset_0).publish_state(id(xg).preset_at_json(0));
            id(pool_color_preset_1).publish_state(id(xg).preset_at_json(1));
            id(pool_color_preset_2).publish_state(id(xg).preset_at_json(2));
            id(pool_color_preset_3).publish_state(id(xg).preset_at_json(3));
            id(pool_color_preset_4).publish_state(id(xg).preset_at_json(4));
            id(pool_color_preset_count).publish_state(
                (float) id(xg).preset_count());

ota:
  - platform: esphome
    password: !secret ota_password

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ssid: "${friendly_name} Fallback"
    password: !secret fallback_ap_password

# Captive portal on the fallback AP so a phone can configure Wi-Fi
# if the primary network is unreachable.
captive_portal:

# ColorSplash XG custom component (issue #10). Scans for the
# controller's advertised local name "BGScripr" and auto-connects.
# Component source lives in ./components/colorsplash_xg/.
external_components:
  - source:
      type: local
      path: components

esp32_ble_tracker:

colorsplash_xg:
  id: xg
  # Optional override for environments with multiple BGScripr
  # peripherals in range (uncommon). When unset, we match the first
  # advertiser whose local name equals BGScripr.
  # mac_address: "AA:BB:CC:DD:EE:FF"

# HA-facing light entity (#11). on/off maps to the controller's
# Standby byte; the 12 canonical effects (5 solids + 7 shows) are
# auto-registered by the colorsplash_xg/light platform. Restore
# to OFF on boot — the fixture's true state isn't readable, so we
# don't pretend to restore the effect; the user re-selects on use.
#
# No `on_state:` lambda here — the display variant uses one to
# update LVGL widget styles, but the headless variant has no
# widgets. HA gets fixture-state updates via the colorsplash_xg
# component's normal indication-echo path.
light:
  - platform: colorsplash_xg
    colorsplash_xg_id: xg
    id: pool_light
    name: "Pool Light"
    restore_mode: ALWAYS_OFF
    # rgb_mode opt-in: power-user toggle for HA-native RGB control
    # (HA's color wheel drives the bridge's embedded LUT picker).
    # Default off — the fixture's real palette is the 12 effects
    # below + user presets via select.pool_active_preset, so
    # exposing arbitrary RGB at the light entity level mis-frames
    # the hardware. The JS card still does the experimental
    # color-picker UX via the pool_set_rgb service.

# Show-control primitives — act on the controller's internal
# "last locked" slot. Lock captures current displayed color;
# Return replays it. The 5 solid colors + 7 shows are all
# exposed as light effects (see light/__init__.py).
button:
  - platform: template
    name: "Pool Color Lock"
    icon: "mdi:lock"
    on_press:
      then:
        - lambda: 'id(xg).send_effect_byte(0x0d);'
  - platform: template
    name: "Pool Color Return"
    icon: "mdi:restore"
    on_press:
      then:
        - lambda: 'id(xg).send_effect_byte(0x0e);'

# Watchdog surface for HA (#12). The binary_sensor mirrors the BLE
# client's connected() state so HA can flag a disconnected fixture
# in automations / dashboards; the text_sensor exposes the most
# recent BLE error so the user has immediate feedback without
# having to open the ESPHome logs.
binary_sensor:
  - platform: template
    name: "Pool BLE Connected"
    id: pool_ble_connected
    device_class: connectivity
    lambda: 'return id(xg).connected();'

text_sensor:
  - platform: template
    name: "Pool Last BLE Error"
    id: pool_last_ble_error
    update_interval: 15s
    lambda: 'return id(xg).last_error();'
  - platform: template
    name: "Pool Last Command"
    id: pool_last_command
    update_interval: 15s
    lambda: 'return id(xg).last_command_name();'
  # Surfaces the controller's most recent indication echo — the
  # authoritative "command accepted" signal per PROTOCOL.md §
  # Controller-to-central indications. Originally added for the
  # Phase 4a probe; kept as a permanent troubleshooting sensor so
  # any future debugging can confirm whether a byte was silently
  # dropped vs. acknowledged.
  - platform: template
    name: "Pool Last Echo"
    id: pool_last_echo
    update_interval: 5s
    lambda: |-
      auto e = id(xg).last_echoed_byte();
      if (!e.has_value()) return std::string("(none)");
      char buf[16];
      std::snprintf(buf, sizeof(buf), "0x%02x", *e);
      return std::string(buf);

  # Most recent recipe returned by the picker — exposed as
  # "0x<byte>,<wait_ms>" so the JS card can save user presets
  # directly as (start_byte, wait_ms) pairs that reproduce the
  # exact color via the pool_scrub service. Empty string until
  # the first pick_color() call. See issue #53.
  - platform: template
    name: "Pool Last Picked Recipe"
    id: pool_last_picked_recipe
    update_interval: 2s
    lambda: |-
      auto rec = id(xg).last_picked_recipe();
      if (!rec.has_value()) return std::string("");
      char buf[32];
      std::snprintf(buf, sizeof(buf), "0x%02x,%u",
                    rec->start_byte, (unsigned) rec->wait_ms);
      return std::string(buf);

  # Saved color preset table (issues #53, #65). Each preset slot
  # is exposed as its own text_sensor so each entity's state
  # stays well under HA's 255-char ceiling — no JSON truncation,
  # no silent disappearance regardless of slug / name length.
  # The card aggregates these 5 slots into the swatch row.
  # All marked entity_category: diagnostic so they hide from the
  # default dashboard view but remain visible on the bridge
  # device page in Settings → Devices & Services.
  - platform: template
    name: "Pool Color Preset 0"
    id: pool_color_preset_0
    entity_category: diagnostic
    update_interval: 30s
    lambda: 'return id(xg).preset_at_json(0);'
  - platform: template
    name: "Pool Color Preset 1"
    id: pool_color_preset_1
    entity_category: diagnostic
    update_interval: 30s
    lambda: 'return id(xg).preset_at_json(1);'
  - platform: template
    name: "Pool Color Preset 2"
    id: pool_color_preset_2
    entity_category: diagnostic
    update_interval: 30s
    lambda: 'return id(xg).preset_at_json(2);'
  - platform: template
    name: "Pool Color Preset 3"
    id: pool_color_preset_3
    entity_category: diagnostic
    update_interval: 30s
    lambda: 'return id(xg).preset_at_json(3);'
  - platform: template
    name: "Pool Color Preset 4"
    id: pool_color_preset_4
    entity_category: diagnostic
    update_interval: 30s
    lambda: 'return id(xg).preset_at_json(4);'

  # The fixture's currently-displayed RGB as a "#rrggbb" string,
  # or empty when the displayed color isn't a steady RGB (shows,
  # standby). Driven by the bridge's color paths (#73 / #75) so
  # the JS card can render its wheel cursor + bulb tint without
  # the light entity exposing rgb_color.
  - platform: template
    name: "Pool Last Displayed Color"
    id: pool_last_displayed_color
    update_interval: 2s
    lambda: 'return id(xg).last_displayed_color_hex();'

# Active user preset (#73). HA scenes capture the option string
# so they can recall a saved preset by slug as part of their
# state snapshot. Options refresh dynamically as the user adds
# / deletes presets via the JS card.
select:
  - platform: colorsplash_xg
    colorsplash_xg_id: xg
    id: pool_active_preset
    name: "Pool Active Preset"

# Numeric diagnostics (#13). RSSI is polled every 30 s while
# connected; the two counters are monotonic-within-session totals
# that reset on reboot.
sensor:
  - platform: template
    name: "Pool BLE RSSI"
    id: pool_ble_rssi
    unit_of_measurement: "dBm"
    device_class: signal_strength
    state_class: measurement
    accuracy_decimals: 0
    update_interval: 30s
    lambda: |-
      int8_t v = id(xg).last_rssi();
      if (v == 0) return NAN;  // 0 = not yet queried / disconnected
      return (float) v;
  - platform: template
    name: "Pool Command Count"
    id: pool_command_count
    state_class: total_increasing
    accuracy_decimals: 0
    update_interval: 30s
    lambda: 'return (float) id(xg).total_commands();'
  - platform: template
    name: "Pool Error Count"
    id: pool_error_count
    state_class: total_increasing
    accuracy_decimals: 0
    update_interval: 30s
    lambda: 'return (float) id(xg).total_errors();'

  # Number of saved user presets, 0..MAX_COLOR_PRESETS (#65). Card
  # uses this as the source of truth for which indexed slots to
  # iterate.
  - platform: template
    name: "Pool Color Preset Count"
    id: pool_color_preset_count
    entity_category: diagnostic
    accuracy_decimals: 0
    update_interval: 30s
    lambda: 'return (float) id(xg).preset_count();'

# ─── Phase 4b PoC — show-scrub primitive ─────────────────────────
# Triggered by the `pool_scrub` HA service defined under api.services
# above. Sends a start byte, waits, then sends Lock (0x0d). Caller
# manages from-state normalization (typically: send Standby, wait
# ~3 s, then call this).
script:
  - id: pool_scrub_seq
    parameters:
      start_byte: int
      wait_ms: int
    then:
      - lambda: |-
          ESP_LOGI("scrub",
                   "start: byte=0x%02x wait_ms=%d",
                   (uint8_t) start_byte, wait_ms);
          id(xg).send_effect_byte((uint8_t) start_byte);
      - delay: !lambda 'return wait_ms;'
      - lambda: |-
          ESP_LOGI("scrub", "lock fire");
          id(xg).send_effect_byte(0x0d);  // Lock
